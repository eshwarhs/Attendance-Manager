<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.attendancemanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ExtraClass"
            android:label="Add Extra Class"/>
        <activity android:name=".overall_stats" />
        <activity android:name=".overall"
            android:label="Overall Attendance"/>
        <activity android:name=".HomeScreen" />
        <activity
            android:name=".subtt"
            android:label="TimeTable" />
        <activity
            android:name=".timetable"
            android:label="TimeTable"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".name" />
        <activity
            android:name=".subjects"
            android:label="Add Subjects" />
        <activity android:name=".gender" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>